export enum GameState{
    PreGame,
    Setup,
    InProgress,
    GameOver
}

export enum CellType {
    Empty = "empty",
    Pellet = "pellet",
    Snake = "snake"
}

export enum GameMode{
    Classic = "Classic",
    Blitz = "Blitz"
}